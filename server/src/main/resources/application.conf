ktor {
    deployment {
        port = 8080
        port = ${?PORT}
    }
    application {
        modules = [org.athletica.crm.ApplicationKt.module]
    }
}

database {
    url = ${POSTGRES_URL}
    user = ${POSTGRES_USER}
    password = ${POSTGRES_PASSWORD}
}

jwt {
    secret = ${?JWT_SECRET}
    accessTokenTtlMinutes = 15
    refreshTokenTtlDays = 30
}

cors {
    allowedHosts = "localhost:8081"
    allowedHosts = ${?CORS_ALLOWED_HOSTS}
}
