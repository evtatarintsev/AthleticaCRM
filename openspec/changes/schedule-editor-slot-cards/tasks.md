## 1. Модель карточки

- [x] 1.1 Завести в `components/groups` `SlotCardId` и `SlotCard(id, days: Set<DayOfWeek>, startAt, endAt, hallId: HallId?)` с KDoc на русском. Проверка: `./gradlew :composeApp:compileKotlinJvm` проходит
- [x] 1.2 Реализовать `List<ScheduleSlot>.toCards()`: группировка по `(startAt, endAt, hallId)`, порядок по первому дню недели, затем по времени начала. Проверка: тесты в `SlotCardTest` — «Пн+Ср зал A и Сб зал B → две карточки», «разный зал → разные карточки», «Пн–Пт 09:00 выше Пн–Пт 18:00»
- [x] 1.3 Реализовать `List<SlotCard>.toSlots()`: разворачивание дней в слоты, карточки без зала пропускаются. Проверка: тест «`toSlots(toCards(x))` совпадает с `x` как множество», тест «три дня → три слота», тест «пять будних дней в двух карточках → десять слотов»

## 2. Состояние диалога и проверки

- [x] 2.1 Заменить `SetScheduleState.slots` на `cards` и `nextCardId`; добавить переходы `withCardAdded(halls)`, `withCardChanged(card)`, `withCardRemoved(id)`, сбрасывающие `error`. Проверка: тесты в `SetScheduleStateTest` на добавление, изменение и удаление по `id`
- [x] 2.2 В `withCardAdded` создавать карточку без дней, с залом только при единственном зале и временем последней карточки (иначе 00:00–01:00). Проверка: тесты «один зал → подставлен», «несколько залов → `null`», «время скопировано из последней карточки»
- [x] 2.3 Завести `SlotCardError` (`NoDays`, `NoHall`, `EndNotAfterStart`, `Duplicate(day, startAt)`) и производное `cardErrors`; `isValid = !isSaving && cardErrors.isEmpty()`. Проверка: тест на каждый тип ошибки, тест «дубль Пн 15:00 помечает обе карточки», тест «пустой список карточек валиден»
- [x] 2.4 Перевести `SetScheduleViewModel`: `initialSlots` → `toCards()`, методы `onCardAdd`, `onCardChange`, `onCardRemove`, в `onSave` отправлять `cards.toSlots()`. Проверка: `./gradlew :composeApp:jvmTest --tests "*SetScheduleStateTest*" --tests "*SlotCardTest*"` зелёный, место вызова диалога не изменилось

## 3. Строки

- [x] 3.1 Добавить `weekday_short_monday…sunday` и тексты ошибок карточки (нет дней, нет зала, конец не позже начала, повтор дня и времени) в `values/strings.xml` и `values-en/strings.xml`. Проверка: `./gradlew :composeApp:generateComposeResClass` проходит, ключи есть в обоих файлах

## 4. UI

- [x] 4.1 Переписать `ScheduleEditor` на список карточек: `OutlinedCard`, ряд из семи переключателей дней (`weight(1f)`, `toggleable`, `Role.Checkbox`, высота 40 dp), ряд времени на `weight(1f)` с кнопкой удаления, зал на `fillMaxWidth`, ошибки из `cardErrors` под карточкой; `key(card.id)` для каждой карточки. Удалить `DayOfWeek.displayName` из редактора. Проверка: компиляция и визуальная проверка на десктопе (4.4)
- [x] 4.2 Кнопку «Добавить слот» сделать доступной всегда; при пустых залах оставить подсказку `schedule_halls_empty`. Проверка: при нуле залов карточка добавляется и показывает ошибку «нет зала»
- [x] 4.3 В `SetScheduleDialog` добавить `verticalScroll` контенту и передать новые колбэки в редактор. Проверка: при 6+ карточках список прокручивается, кнопки «Сохранить»/«Отмена» видны
- [x] 4.4 Проверить диалог в web-превью на ширине 360 dp и 320 dp: все семь дней, оба поля времени целиком, выбор зала и кнопка удаления видны и нажимаются; на широком экране длинное название зала не обрезается. Проверка: скриншоты приложены к PR

## 5. Завершение

- [x] 5.1 Пройти сценарий целиком: открыть группу с «Пн Ср Пт 15:00–17:00», снять среду, сохранить — на неделе после даты вступления в силу среды нет, понедельник и пятница остались; повторное сохранение без правок не меняет расписание. Проверка: результат на странице группы и в недельном расписании
- [x] 5.2 Выполнить `./gradlew build` и `./gradlew ktlintFormat`. Проверка: обе команды завершаются успешно, `git diff` после ktlintFormat пуст
