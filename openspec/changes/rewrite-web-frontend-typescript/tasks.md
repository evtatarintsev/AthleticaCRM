## 1. Фаза 0а: строгость TypeScript

- [x] 1.1 Вынести общие опции в `web/tsconfig.base.json` и добавить флаги из design §1 (`exactOptionalPropertyTypes`, `noPropertyAccessFromIndexSignature`, `noImplicitOverride`, `noImplicitReturns`, `allowUnreachableCode: false`, `allowUnusedLabels: false`, `erasableSyntaxOnly`, `noUncheckedSideEffectImports`, `forceConsistentCasingInFileNames`, `libReplacement: false`, `skipLibCheck: false`); `tsconfig.app.json`, `tsconfig.node.json` расширяют базу. Проверка: `npx tsc -b` показывает ошибки `exactOptionalPropertyTypes` в `src/ui/fields.tsx` — флаги действуют
- [x] 1.2 Завести `tsconfig.test.json` для `**/*.test.ts(x)`: убрать `vitest/globals` и `@testing-library/jest-dom` из `types` приложения, в тестах импортировать `describe/it/expect/vi` из `vitest` явно. Проверка: `npx tsc -b` не сообщает `TS2428 All declarations of 'Assertion'`; если конфликт остался, `skipLibCheck: true` только в `tsconfig.test.json` с комментарием, у `tsconfig.app.json` он остаётся `false`
- [x] 1.3 Исправить существующий код под новые флаги (`fields.tsx`: `hint?: string | undefined` или условная передача пропа). Проверка: `npx tsc -b` без ошибок
- [x] 1.4 Переписать `web/eslint.config.js` по design §2: `strictTypeChecked` + `stylisticTypeChecked` с `projectService`, `consistent-type-assertions: never`, `ban-ts-comment` без исключений, `switch-exhaustiveness-check` (`requireDefaultForNonUnion`, `considerDefaultExhaustiveForUnions: false`), `strict-boolean-expressions`, `linterOptions.noInlineConfig` и `reportUnusedDisableDirectives: "error"`; скрипт `lint` = `eslint . --max-warnings 0`. Проверка: временный файл с `as`, `!`, `// @ts-expect-error`, `// eslint-disable-next-line` и потерянным промисом даёт по ошибке на каждую конструкцию; файл удалён
- [x] 1.5 Убрать нарушения в текущем коде: `response.json() as Res`, `field("currency") as Currency`, `getElementById("root")!`, `vi.fn()` без типизации в тестах (моки через фабрики с `satisfies`). Проверка: `npm run lint` без ошибок
- [x] 1.6 Добавить `type-coverage` (`--strict --at-least 100 --detail`) в `npm run check`; порядок команд — design §4. Проверка: `npm run check` зелёный; временное `JSON.parse(x).y` роняет `type-coverage`
- [x] 1.7 Добавить в `.github/workflows` job на PR, запускающий `npm ci && npm run check` в `web/`. Проверка: job виден в PR этой фазы и зелёный

## 2. Фаза 0б: контракты API

- [x] 2.1 В `RouteWithContext` записывать в атрибуты `Route` `ApiContract(method, requestSerializer, responseSerializer, requestIn)` для всех вариантов `get`/`post`; для маршрутов `/auth/branches`, `/auth/login`, `/auth/sign-up`, `/auth/refresh-token`, `/checkout/*` — явная запись рядом с объявлением. KDoc на русском. Проверка: серверный тест обходит маршруты `testApplication` и находит контракт у каждого JSON-маршрута под `/api`
- [x] 2.2 Включить `encodeDefaults = true` в `Json` сервера (`Application.kt`). Проверка: тест, что ответ со значением по умолчанию содержит поле; `./gradlew test` зелёный, упавшие сравнения JSON обновлены осознанно
- [x] 2.3 Завести source set `contracts` в `server` и задачу `:server:generateWebContracts` (JavaExec): поднимает приложение как тесты (Testcontainers), обходит маршруты, проверяет инварианты design §5 (контракт есть у каждого маршрута, GET-запросы плоские). Проверка: задача падает с понятным сообщением на маршруте без контракта (временная проверка) и проходит на текущем сервере
- [x] 2.4 Реализовать обход `SerialDescriptor` и генерацию zod по таблице design §6: примитивы, `Long` в safe-integer, идентификаторы с брендом, `Instant`/`LocalDate`/`LocalTime` с брендами ISO, `DayOfWeek`, enum, nullable, коллекции `readonly`, `Map`, sealed и открытый полиморфизм через `appSerializersModule`, таблица своих сериализаторов, ошибка на неизвестный сериализатор и на совпадение простых имён. Проверка: юнит-тесты генератора на каждый вид типа, включая два негативных
- [x] 2.5 Реализовать разделение ролей: обход графа от эндпоинтов, поля с дефолтом необязательны только в запросах, `XxxInput` для типов в обеих ролях; сгенерировать объект `endpoints`. Проверка: юнит-тест на тип в обеих ролях; сгенерированный `web/src/api/generated/contracts.ts` проходит `npm run check`, включая `type-coverage` 100%
- [x] 2.6 Добавить `npm run contracts` (вызов Gradle-задачи из корня) и CI-job `contracts`: генерация + `git diff --exit-code web/src/api/generated`. Проверка: PR с изменённой Kotlin-схемой без перегенерации падает на этом job
- [x] 2.7 Написать клиент `web/src/api/client.ts` поверх `endpoints` (design §6, §10, §11): вывод типов из манифеста, `safeParse` ответа, `ApiResult` с ошибками `business | unauthenticated | unavailable | contract`, `Accept-Language`, single-flight refresh на 401 с одним повтором, лог нарушения контракта в консоль. Проверка: vitest — перепутанный `ClientId`/`GroupId` не компилируется (тест типов через `expectTypeOf`), лишнее поле отбрасывается, поле не того типа даёт `contract`, три параллельных 401 → один refresh, неудачный refresh → `unauthenticated`
- [x] 2.8 Удалить ручные схемы `web/src/api/schemas.ts`, перевести вход и регистрацию на сгенерированные типы. Проверка: `rg "interface .*Request|interface .*Response" web/src` пусто, существующие тесты входа и регистрации зелёные

## 3. Фаза 0в: каркас приложения

- [x] 3.1 Заменить React Router на TanStack Router (code-based): корневой маршрут, `BASE_PATH` из константы сборки, маршруты `/login`, `/sign-up`, `/` и страница «не найдено»; параметры и search-параметры с zod-валидацией. Проверка: ссылка на несуществующий маршрут или без обязательного параметра не компилируется (тест типов); `/web/clients/not-a-uuid` показывает «не найдено» без запроса к API
- [x] 3.2 Подключить TanStack Query: `QueryClient`, адаптер `ApiResult` → исключение в одном месте, фабрики ключей с филиалом в ключе. Проверка: vitest — после смены филиала запрос с тем же эндпоинтом идёт заново, старые данные не отдаются
- [x] 3.3 Реализовать i18n (design §9): `ru` как источник ключей, `en` типа `Messages<typeof ru>`, типизированные параметры, `Intl.PluralRules`, выбор языка по браузеру с ручным переключением в `localStorage` (обращения в `try/catch`), форматирование дат, чисел и `Money`. Перенести строки входа и регистрации, английские тексты — из `composeApp/.../values-en/strings.xml`. Проверка: удаление ключа из `en` ломает `tsc`; тест — `en-US` → английский, `de-DE` → русский
- [x] 3.4 Подключить shadcn/ui (Radix) и привести добавленные компоненты к строгим правилам (без `as`, `!`, с `exactOptionalPropertyTypes`). Завести базовый набор: Button, Input, Select, Checkbox, Dialog, Sheet, DropdownMenu, Table, Tabs, Toast, Skeleton. Проверка: `npm run check` зелёный
- [x] 3.5 Подключить TanStack Form с zod-валидацией, завести обёртки полей с `autocomplete` и доступными ошибками. Перевести формы входа и регистрации. Проверка: тесты входа и регистрации зелёные; неверный путь поля в форме не компилируется
- [x] 3.6 Сделать защищённый layout: проверка сессии через `/auth/me`, редирект на `/login?redirect=` с валидацией относительного пути, боковая навигация ≥ 1024 px и нижняя/выезжающая на узких экранах, меню аккаунта, реестр `migratedSections` со ссылками на KMP-клиент для неперенесённых разделов. Проверка: тесты — `redirect=https://evil.example/` ведёт на главную; неперенесённый раздел рендерится как `<a href="/tasks">`; в браузере на 360 px нет горизонтальной прокрутки
- [x] 3.7 После входа вести на главную веб-клиента, если раздел «Главная» перенесён, иначе на `/` KMP-клиента; `redirect` имеет приоритет. Проверка: тест обоих вариантов
- [ ] 3.8 Выкатить фазу 0. Проверка: на проде `/web/login` входит, `/` KMP-клиента открывается без повторного входа, `/api/auth/me` отдаёт поля с дефолтами

## 4. Фаза 1: профиль и аккаунт

- [x] 4.1 Экран профиля (`/settings/edit-profile`) с аватаром через загрузку файла. Проверка: изменение имени и аватара видно после перезагрузки и в KMP-клиенте
- [x] 4.2 Смена пароля (`/settings/change-password`) с `autocomplete="current-password"` / `"new-password"`. Проверка: вход с новым паролем проходит, со старым — нет
- [x] 4.3 Смена филиала из меню аккаунта и страницы `/settings/switch-branch`: запрос переключения, `queryClient` сбрасывается. Проверка: сценарий «Смена филиала» из спеки `app-shell` в браузере
- [x] 4.4 Выход: `/auth/logout`, `queryClient.clear()`, переход на вход. Проверка: сценарий «Выход и кнопка „Назад“»
- [x] 4.5 Колокольчик уведомлений: список, отметка прочитанными. Проверка: уведомление, созданное на сервере, появляется и после отметки не считается непрочитанным

## 5. Фаза 2: справочники в настройках

- [x] 5.1 Общие компоненты списка-справочника и формы элемента на TanStack Table/Form, под ними — залы, дисциплины, источники клиентов. Проверка: создание, переименование и удаление элемента в каждом справочнике; результат виден в KMP-клиенте
- [x] 5.2 Тарифы. Проверка: паритет с `TariffsScreen` — все поля и действия, суммы в `Money` отображаются в валюте организации
- [x] 5.3 Филиалы и редактирование филиала. Проверка: паритет с `BranchesScreen`/`BranchEditScreen`
- [x] 5.4 Дополнительные поля клиентов (все типы определений, включая `Select`). Проверка: поле каждого типа создаётся и появляется в карточке клиента KMP-клиента
- [x] 5.5 Роли: список и создание с набором прав. Проверка: паритет с `RolesScreen`/`RoleCreateScreen`, сотрудник с новой ролью получает права
- [x] 5.6 Страница настроек `/settings` со ссылками на перенесённые пункты и ссылками в KMP-клиент для остальных. Проверка: все пункты открываются
- [ ] 5.7 Выкатить фазу 2. Проверка: смоук справочников на проде

## 6. Фаза 3: клиенты

- [x] 6.1 Список клиентов: фильтры и сортировка в search-параметрах, настройка колонок (`ClientDisplaySettings`), мобильная карточка вместо таблицы на узком экране, множественный выбор. Проверка: фильтр переживает перезагрузку; паритет фильтров с `ClientsFilterContent`
- [x] 6.2 Создание и редактирование клиента: основные поля, контакты, дополнительные поля. Проверка: клиент с полями всех типов создаётся и редактируется
- [ ] 6.3 Карточка клиента: сведения, заметки, документы, контакты, дополнительные поля. Проверка: паритет с `ClientDetailScreen`
- [ ] 6.4 Баланс: корректировка и история. Проверка: корректировка меняет баланс, запись видна в истории, без права `CAN_VIEW_CLIENT_BALANCE` блок не показывается
- [ ] 6.5 Абонементы: выдача (`/clients/{id}/issue-subscription`) и история. Проверка: выданный абонемент виден в истории и на сервере
- [ ] 6.6 Истории посещений и платежей. Проверка: данные совпадают с KMP-клиентом на том же клиенте
- [ ] 6.7 Добавление в группу и экспорт выбранных клиентов. Проверка: клиент появляется в группе; файл экспорта скачивается и открывается
- [ ] 6.8 Включить раздел `clients` в `migratedSections` и выкатить. Проверка: сценарий «Раздел ещё не перенесён» из спеки `app-shell` — из карточки клиента ссылки в группы ведут в KMP-клиент

## 7. Фаза 4: группы и расписание

- [ ] 7.1 Список групп с фильтрами, создание, редактирование, карточка. Проверка: паритет с `GroupsScreen`/`GroupDetailScreen`
- [ ] 7.2 Тренеры и дисциплины группы. Проверка: добавление и удаление отражаются в карточке
- [ ] 7.3 Редактор расписания группы на карточках слотов. Проверка: автотесты на каждый сценарий `openspec/specs/group-schedule-editor/spec.md`; в браузере на 320 и 360 CSS px видны все элементы карточки
- [ ] 7.4 Недельная сетка расписания с выбором недели и фильтрами. Проверка: сценарии `openspec/specs/schedule/spec.md`, относящиеся к интерфейсу (сетка, выбор недели, состав карточки, цвет с сервера, фильтры)
- [ ] 7.5 Решить открытый вопрос о `Temporal` (design, Open Questions) до 7.3 и зафиксировать решение в design.md. Проверка: запись в design.md
- [ ] 7.6 Включить разделы `groups` и `schedule`, выкатить. Проверка: смоук на проде

## 8. Фаза 5: сотрудники

- [ ] 8.1 Список с фильтрами, карточка, создание, редактирование с аватаром и ролями. Проверка: паритет с экранами `components/employees`
- [ ] 8.2 Отправка доступа. Проверка: сотрудник получает письмо и входит
- [ ] 8.3 Включить раздел `employees`, выкатить. Проверка: смоук на проде

## 9. Фаза 6: задачи

- [ ] 9.1 Список задач с фильтрами, статусами и видимостью по праву `CAN_VIEW_ALL_TASKS`. Проверка: сотрудник без права видит только свои задачи
- [ ] 9.2 Карточка и создание задачи, назначение исполнителя, вложения (загрузка и скачивание). Проверка: вложение загружается и открывается по подписанной ссылке
- [ ] 9.3 Массовые действия. Проверка: смена статуса нескольких задач; отказ, если к одной нет прав
- [ ] 9.4 Включить раздел `tasks`, выкатить. Проверка: смоук на проде

## 10. Фаза 7: главная, сообщения, остальные настройки

- [ ] 10.1 Главная: виджеты дней рождения и должников, настройка виджетов. Проверка: паритет с `HomeScreen` и `DashboardSettingsDialog`
- [ ] 10.2 Переписка с клиентом. Проверка: сообщение отправляется, входящее появляется
- [ ] 10.3 Каналы. Проверка: паритет с `ChannelsScreen`
- [ ] 10.4 Основные настройки организации. Проверка: паритет с `OrgBasicSettingsScreen`
- [ ] 10.5 Баланс организации и пополнение. Проверка: переход к оплате ЮKassa и возврат на страницу результата
- [ ] 10.6 Импорт клиентов с сопоставлением колонок (`ImportTarget`, `LeadSourceAction`). Проверка: импорт тестового файла создаёт клиентов с источниками и доп. полями
- [ ] 10.7 Журнал действий. Проверка: действие, выполненное в веб-клиенте, видно в журнале
- [ ] 10.8 Включить оставшиеся разделы, выкатить. Проверка: все значения `migratedSections` равны `true`, таблица паритета из спеки `migration` пройдена вручную

## 11. Фаза 8: переключение `/`

- [ ] 11.1 `BASE_PATH = ""`, убрать реестр `migratedSections` и ссылки в KMP-клиент. Проверка: `rg "migratedSections" web/src` пусто, `npm run check` зелёный
- [ ] 11.2 `nginx/prod.conf.template` и `nginx/dev.conf`: `/` → `frontend`, `/web/...` → 301 на путь без префикса; nginx внутри образа `frontend` отдаёт SPA от корня. Проверка: `nginx -t` для обоих конфигов; локально в образе `/clients/<id>` → 200, `/web/clients` → 301 на `/clients`
- [ ] 11.3 Выкатить. Проверка: сценарии «Закладка из KMP-клиента» и «Старая ссылка с префиксом» на проде

## 12. Фаза 9: удаление wasm-клиента

- [ ] 12.1 Удалить wasm-таргет из `composeApp/build.gradle.kts` и `webMain`, `Dockerfile.web`, `nginx.conf` в корне, сервис `web` из `docker-compose.prod.yaml`, job `web` из `.github/workflows/docker.yml`, ветку `--only web` из `deploy_to_stand.sh`. Проверка: `./gradlew build` проходит и собирает jvm-клиент; `bash -n deploy_to_stand.sh`; `rg -i "wasmJs|Dockerfile.web" --glob '!openspec/**'` пусто
- [ ] 12.2 Выкатить не раньше чем через неделю после 11.3. Проверка: на проде нет контейнера `web`, `/` работает

## 13. Документация и завершение

- [x] 13.1 В фазе 0 обновить `CLAUDE.md`: правила TS-кода (без `as`, `!`, `any`; исключения только в конфиге), `npm run contracts` после изменения схем, запрет новых экранов в KMP-вебе. Обновить `web/README.md` примерами типовых приёмов (декодирование, сужение, исчерпывающий `switch`). Проверка: разделы присутствуют
- [ ] 13.2 На фазах 8–9 обновить `DEPLOY.md` и `CLAUDE.md` (структура модулей, сервисы). Проверка: упоминаний wasm-клиента и `/web/` как временного пути не осталось
- [ ] 13.3 В каждом PR фазы: `./gradlew build`, `./gradlew ktlintFormat`, `npm run check` в `web/`. Проверка: команды зелёные, `git diff` после `ktlintFormat` пуст
