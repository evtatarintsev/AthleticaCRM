## 1. Схема БД

- [x] 1.1 Миграция: `CREATE EXTENSION IF NOT EXISTS btree_gist` и `IMMUTABLE`-функция `isodow_of(day_of_week) RETURNS int`; проверить `SELECT isodow_of('MONDAY')` = 1 и `isodow_of('SUNDAY')` = 7 в тесте на TestContainers
- [x] 1.2 Миграция: `schedule_slots.validity daterange NOT NULL` с заполнением существующих строк `daterange(<прошлая дата>, NULL)`; проверить, что после миграции все слоты действуют на сегодня
- [x] 1.3 Миграция: exclusion constraint `no_overlapping_slot_versions` по `(group_id, day_of_week, start_time, validity)`; тест проверяет, что вставка пересекающейся версии отклоняется, а непересекающейся — проходит
- [x] 1.4 Миграция: `sessions.origin_slot_id UUID REFERENCES schedule_slots (id) ON DELETE RESTRICT` с заполнением по совпадению `(group_id, origin_day_of_week, origin_start_time)`; тест проверяет, что у сгенерированных занятий ссылка проставлена
- [x] 1.5 Миграция: уникальный индекс перестроен на `(origin_slot_id, origin_date)`, удалены `origin_day_of_week`, `origin_start_time`, `is_manual`, `uq_sessions_generated`; тест проверяет отклонение дубля по новой паре
- [x] 1.6 Миграция: `sessions.group_id` переведён с `ON DELETE SET NULL` на `ON DELETE RESTRICT`; тест проверяет, что удаление группы с занятиями отклоняется
- [x] 1.7 Прогнать `./gradlew test --tests "*read*"` и убедиться, что существующие DB-тесты поднимаются на новой схеме

## 2. Доменные типы расписания

- [x] 2.1 Ввести `SlotId` в `shared/.../core/entityids/`; проверить компиляцией и тестом сериализации по образцу существующих id
- [x] 2.2 Ввести value-тип `Validity` со smart constructor (`of(from, to): Either<DomainError, Validity>`), методами `contains` и `closedAt`; тест покрывает отклонение `to <= from`, границы включительно/исключительно и бессрочный период
- [x] 2.3 Превратить `domain/groups/ScheduleSlot` в сущность с `SlotId` и `Validity`; проверить компиляцией

## 3. Расписание как самостоятельная сущность

- [x] 3.1 Завести интерфейс `GroupSchedule` (`slotsOn`, `slotsDuring`, `setFrom`) в `domain/groups/`; метода без момента времени не добавлять
- [x] 3.2 Реализовать `DbGroupSchedule.slotsOn` и `slotsDuring`; DB-тест: группа с закрытой и действующей версией слота возвращает только подходящую под дату
- [x] 3.3 Реализовать `DbGroupSchedule.setFrom` — закрытие исчезнувших, замена изменившихся, сохранение совпавших; DB-тест на каждый из трёх случаев отдельно
- [x] 3.4 Реализовать в `setFrom` правило «с даты D и навсегда»: удаление версий, начинающихся в D или позже, обрезка пересекающих D; DB-тест «отложенное изменение с 1 июня отменяется установкой с 1 марта»
- [x] 3.5 Отклонять `effectiveFrom` в прошлом, по умолчанию подставлять сегодня; тест на обе ветки
- [x] 3.6 Поддержать пустой набор слотов как закрытие расписания и повторное открытие более поздней датой; DB-тест «летний перерыв и возобновление»
- [x] 3.7 Валидировать входной набор на дубли `(dayOfWeek, startAt)` и на `endAt > startAt`; тест на отклонение обоих случаев

## 4. Отделение расписания от группы

- [x] 4.1 Убрать поле `schedule` из `domain/groups/Group` и из `DbGroup`; проверить компиляцией
- [x] 4.2 Убрать из `DbGroup.save()` блок `DELETE FROM schedule_slots` + повторная вставка; DB-тест «переименование группы не меняет её расписание и историю версий»
- [x] 4.3 Убрать расписание из `Groups.new`; создание группы с расписанием выполняется вызовом `setFrom` следом, в той же транзакции; DB-тест на создание группы с расписанием и без
- [x] 4.4 Убрать расписание из `GroupCreateRequest` и `EditGroupRequest`, добавить `SetGroupScheduleRequest` с `effectiveFrom`; проверить сборкой `shared`
- [x] 4.5 Добавить роут `POST /groups/set-schedule`; проверить ответ на установку расписания с будущей датой и отклонение даты в прошлом
- [x] 4.6 Добавить в схему слота в ответах период действия; проверить сборкой `shared` и обновлением существующих тестов проекций

## 5. Сверка расписания и занятий

- [x] 5.1 Завести интерфейс `ScheduleSync` с единственным методом в `domain/sessions/`; горизонт вычисляется внутри реализации, параметра границы у метода нет
- [x] 5.2 Реализовать шаг «создать недостающие» одним `INSERT ... SELECT` с `generate_series` и `ON CONFLICT DO NOTHING`; DB-тест: занятия созданы на все подходящие даты периода слота внутри горизонта и отсутствуют за ним
- [x] 5.3 Реализовать предикат нетронутости и шаг «обновить атрибуты нетронутых»; DB-тест: смена зала и времени окончания обновляет нетронутые занятия, не меняя их количества и дат
- [x] 5.4 Реализовать шаг «удалить нетронутые вне периода слота»; DB-тест: исключение слота удаляет нетронутые будущие занятия, не оставляя отменённых
- [x] 5.5 Реализовать шаг синхронизации состава тренеров у нетронутых занятий; DB-тест: смена тренеров группы меняет будущие занятия и не трогает прошедшие
- [x] 5.6 DB-тест на неприкосновенность изменённых человеком занятий: отменённое, перенесённое, с переопределёнными тренерами, с заметками — каждое по отдельности переживает смену расписания
- [x] 5.7 DB-тест на неизменность прошедших занятий при полной замене расписания
- [x] 5.8 DB-тест на идемпотентность: две сверки подряд без изменений не меняют количество занятий
- [x] 5.9 DB-тесты граничных дат: смена ровно на дату занятия, смена между занятиями, слот целиком в прошлом, слот целиком за горизонтом, закрытие расписания в ноль
- [x] 5.10 Реализовать открепление занятий при удалении версии слота (`DELETE` нетронутых, `UPDATE origin_slot_id = NULL` остальным); DB-тест: удаление ещё не вступившей в силу версии с ручной правкой внутри горизонта проходит, а пропуск шага роняет транзакцию на FK

## 6. Триггеры сверки и удаление мёртвого кода

- [x] 6.1 Вызвать `ScheduleSync` синхронно в транзакции создания группы, изменения расписания и изменения состава тренеров; тест: сразу после создания группы список занятий их возвращает
- [x] 6.2 Перевести `generateSessionsDaily` в `Application.kt` на `ScheduleSync` вместо цикла по дням; проверить, что ежедневная сверка приводит занятия в соответствие после рассогласования
- [x] 6.3 Удалить `POST /groups/{groupId}/schedule`, `usecases/sessions/UpdateGroupSchedule.kt`, `UpdateGroupScheduleRequest`; проверить сборкой
- [x] 6.4 Удалить `GroupCreatedHandler`, `GroupScheduleChangedHandler`, события `GroupCreated` и `GroupScheduleChanged` и их регистрацию в `Di.kt`; шину `domain/events/` оставить
- [x] 6.5 Удалить `Sessions.futureScheduledBySlot` и `Sessions.syncFutureEmployeesFromGroup` вместе с реализациями; проверить сборкой
- [x] 6.6 Удалить `usecases/sessions/GenerateSessions.kt` и `UpdateGroupEmployees.kt`, перенеся оставшуюся логику в домен; проверить сборкой
- [x] 6.7 Убрать `Session.isManual` как хранимое поле, вывести из `originSlotId == null`; проверить, что схемы ответов сохраняют прежний признак

## 7. Чтение занятий

- [x] 7.1 Убрать генерацию из `sessionList`; проверить тестом, что запрос списка не меняет количество занятий в базе
- [x] 7.2 Перенести `sessionList` в `read/sessions/` как проекцию по правилам CLAUDE.md, зарегистрировать в `ReadViews`; DB-тест в `server/src/test/kotlin/org/athletica/crm/read/sessions/`
- [x] 7.3 Сделать `SessionListRequest.from/to` обязательными и ограничить длину периода; тест: запрос без дат и запрос со слишком длинным периодом отклоняются с ошибкой
- [x] 7.4 Тест-регрессия на исходную уязвимость: `GET /api/sessions/list` без параметров отклоняется и не приводит к записи в БД

## 8. Проекции групп

- [x] 8.1 Добавить фильтр по дате в подзапрос слотов `DbGroupListView`; DB-тест: группа с закрытой версией слота показывает только действующие
- [x] 8.2 Добавить фильтр по дате в подзапрос слотов `DbGroupDetailView`; DB-тест аналогично
- [x] 8.3 Отдавать в проекции сведения о ближайшем запланированном изменении расписания; DB-тест на группу с отложенным изменением и без

## 9. Frontend (composeApp)

- [x] 9.1 Выделить редактирование расписания из формы группы в отдельный экран или диалог с выбором даты вступления в силу; проверить запуском приложения
- [x] 9.2 Показывать предупреждение об отмене ранее запланированного изменения при установке расписания более ранней датой; проверить на группе с отложенным изменением
- [x] 9.3 Показывать на карточке группы сведения о запланированном изменении расписания; проверить на группе с отложенным изменением
- [x] 9.4 Проверить, что все добавленные строки взяты из `stringResource` и заведены во всех `values-*/strings.xml`

## 10. Завершение

- [x] 10.1 Прогнать `./gradlew build` — сборка зелёная
- [x] 10.2 Прогнать `./gradlew ktlintFormat` и убедиться, что правок после него не осталось
- [x] 10.3 Прогнать `./gradlew test` целиком и убедиться, что ранее существовавшие тесты не сломаны
- [x] 10.4 Согласовать порядок слияния с `add-schedule-week-view` и при необходимости снять оттуда материализацию в границах запроса, сохранив ограничение периода
