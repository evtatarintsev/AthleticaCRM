## 1. Контракт в shared

- [ ] 1.1 Добавить перечисление статуса занятия (`scheduled`/`completed`/`cancelled`) в `shared/.../core`; проверка — `./gradlew :shared:build` проходит, существующие схемы занятий не тронуты
- [ ] 1.2 Добавить перечисление ключей палитры `SessionColorKey` (восемь значений из текущей заглушки экрана); проверка — значения сериализуются строками, тест сериализации в `shared` проходит
- [ ] 1.3 Добавить схемы `ScheduleListRequest`, `ScheduleListResponse`, `ScheduleSessionSchema`, `ScheduleHallSchema`, `ScheduleCoachSchema`, `ScheduleDisciplineSchema` в `shared/.../api/schemas/schedule/`; проверка — компиляция `shared` под JVM и wasm, в схемах нет полей вместимости, записанных и свободных мест

## 2. Проекция расписания в домене

- [ ] 2.1 Объявить доменный query-объект `ScheduleQuery(from, to, hallIds, disciplineIds, employeeIds)` и интерфейс проекции по образцу `ClientListQuery`/`ClientListView`; проверка — интерфейс не импортирует ничего из `api.schemas`
- [ ] 2.2 Реализовать выборку одним SQL: `sessions` JOIN `groups` для филиала и названия группы, ограничение периодом, фильтры `hall_id = ANY(...)`, `EXISTS(session_employees ...)`, `EXISTS(group_disciplines ...)`, исключение занятий с `group_id IS NULL`; проверка — тест на TestPostgres возвращает ожидаемый набор занятий за период
- [ ] 2.3 Тест ограничения филиалом: занятия группы другого филиала не попадают в выдачу; проверка — тест на TestPostgres проходит
- [ ] 2.4 Тест семантики фильтров: несколько значений одного фильтра объединяются по «или», разные фильтры — по «и»; проверка — тест на TestPostgres проходит
- [ ] 2.5 Тест фильтра по тренеру с переопределённым составом: занятие, где тренер группы заменён на другого, находится по тренеру занятия и не находится по тренеру группы; проверка — тест на TestPostgres проходит
- [ ] 2.6 Тест исключения осиротевшего занятия (`group_id IS NULL`); проверка — тест на TestPostgres проходит

## 3. Резолвер цвета

- [ ] 3.1 Реализовать `colorKeyFor(group, disciplines)` с тремя ступенями — цвет группы, цвет первой дисциплины, запасной из младших битов UUID по модулю размера палитры; сегодня исполняется только третья ступень; проверка — функция не использует `hashCode()`
- [ ] 3.2 Тесты резолвера: ключ стабилен между вызовами, занятия одной дисциплины получают одинаковый ключ, занятие без дисциплины получает ключ по группе; проверка — `./gradlew :server:test --tests "*ScheduleColor*"` проходит

## 4. Юзкейс и роут

- [ ] 4.1 Валидация периода до обращения к БД: `from <= to` и длина не больше 62 дней, иначе `CommonDomainError`; проверка — тест подтверждает, что при отклонении занятия не создаются и не возвращаются
- [ ] 4.2 Материализация занятий в границах запрошенного окна по группам филиала с передачей уже загруженной группы в генерацию вместо повторной выборки по идентификатору; проверка — тест на TestPostgres подтверждает, что занятия за пределами окна не созданы
- [ ] 4.3 Тест идемпотентности: повторный запрос того же периода не создаёт дубликатов и возвращает тот же состав; проверка — тест на TestPostgres проходит
- [ ] 4.4 Сборка ответа в роуте: догрузка каталогов залов, дисциплин и сотрудников пачками и склейка в схемы ответа; проверка — число запросов к каталогам не зависит от числа занятий
- [ ] 4.5 Зарегистрировать `POST /api/schedule/list` и подключить зависимости в `Di.kt`/`Application.kt`; проверка — запрос к поднятому серверу возвращает занятия периода с названиями зала, тренеров и дисциплин

## 5. API-клиент

- [ ] 5.1 Добавить `ScheduleApiClient` с методом запроса расписания и зарегистрировать его в `ApiClient`; проверка — `./gradlew :shared:build` проходит, клиент доступен как `api.schedule`

## 6. Страница расписания

- [ ] 6.1 Ввести `ScheduleLoadState` (`Loading`/`Loaded`/`Error`) и перевести `ScheduleState` на единый снимок с неделей, данными, фильтрами и справочниками; проверка — в ViewModel остаётся ровно одна изменяемая ячейка состояния
- [ ] 6.2 Загружать занятия при открытии и перезапрашивать при переключении недели, сохраняя выбранные фильтры; проверка — тест ViewModel в `commonTest` подтверждает перезапрос с теми же фильтрами
- [ ] 6.3 Загружать справочники залов, дисциплин и сотрудников один раз при открытии страницы; проверка — переключение недели не приводит к повторной загрузке каталогов
- [ ] 6.4 Панель фильтров: три фильтра с множественным выбором и сбросом; проверка — значение, отсутствующее в занятиях видимой недели, остаётся доступным для выбора
- [ ] 6.5 Карточка занятия: время, продолжительность, название группы, тренеры, зал, дисциплины; строки без данных опускаются целиком, несколько значений перечисляются в одну строку с усечением; проверка — карточка занятия без тренера и дисциплин не показывает пустых подписей
- [ ] 6.6 Продолжительность выводится без нулевой части — только часы либо только минуты; проверка — тест форматирования покрывает 20 минут, 1 час и 1 час 30 минут
- [ ] 6.7 Отменённые занятия отображаются приглушённо и визуально отличимы от запланированных; проверка — видно на экране при наличии отменённого занятия в периоде
- [ ] 6.8 Цвет карточки берётся из `SessionColorKey`, неизвестный ключ рисуется нейтральным; проверка — подстановка неизвестного значения не ломает отрисовку карточки
- [ ] 6.9 Оставить бейдж свободных мест как заглушку с фиксированным текстом, формируемым на клиенте; проверка — текст не приходит с сервера и одинаков на всех карточках
- [ ] 6.10 Состояния страницы: индикатор загрузки, сообщение об ошибке с повтором, сообщение о пустой неделе и о пустом результате фильтрации; проверка — каждое состояние воспроизводится на экране
- [ ] 6.11 Удалить `ScheduleStubData.kt` и остатки локальной генерации данных; проверка — поиск по проекту не находит ссылок на заглушечные данные расписания
- [ ] 6.12 Добавить строки локализации в `values/strings.xml` и `values-en/strings.xml`; проверка — в коде страницы нет литеральных пользовательских строк

## 7. Приёмка

- [ ] 7.1 Прогнать `./gradlew build` и `./gradlew ktlintFormat`; проверка — обе команды завершаются успешно
- [ ] 7.2 Проверить страницу на поднятом сервере: сетка недели, переключение недель, три фильтра, пустая неделя, ошибка загрузки; проверка — сценарии из `specs/schedule/spec.md` воспроизводятся вручную
- [ ] 7.3 Проверить вёрстку на узком экране: фильтры и сетка остаются доступными; проверка — страница используема при ширине телефона
