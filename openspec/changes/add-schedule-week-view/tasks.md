## 1. Контракт в shared

- [x] 1.1 Добавить перечисление статуса занятия (`scheduled`/`completed`/`cancelled`) в `shared/.../core`; проверка — `./gradlew :shared:build` проходит, существующие схемы занятий не тронуты
- [x] 1.2 Добавить перечисление ключей палитры `SessionColorKey` (восемь значений из текущей заглушки экрана); проверка — значения сериализуются строками, тест сериализации в `shared` проходит
- [x] 1.3 Добавить схемы `ScheduleListRequest`, `ScheduleListResponse`, `ScheduleSessionSchema`, `ScheduleGroupSchema`, `ScheduleHallSchema`, `ScheduleCoachSchema`, `ScheduleDisciplineSchema` в `shared/.../api/schemas/schedule/`; проверка — компиляция `shared` под JVM и wasm, в схемах нет полей вместимости, записанных и свободных мест

## 2. Проекция `read/schedule/`

- [x] 2.1 Объявить `read/schedule/ScheduleView.kt`: интерфейс `ScheduleView` с методом `list(query): ScheduleListResponse` и `ScheduleQuery(from, to, hallIds?, disciplineIds?, employeeIds?)`; проверка — файл не импортирует ничего из `domain/**`
- [x] 2.2 Реализовать `DbScheduleView` одним SQL: `sessions` JOIN `groups` (скоуп `g.branch_id = ctx.branchId`, название группы) JOIN `halls` (название зала), `jsonb_agg` тренеров занятия из `session_employees` и дисциплин группы из `group_disciplines` с детерминированным порядком, ограничение периодом, фильтры `hall_id = ANY(...)`, `EXISTS(session_employees ...)`, `EXISTS(group_disciplines ...)` только для заданных фильтров; маппер строки собирает `ScheduleSessionSchema` напрямую; проверка — тест на TestPostgres возвращает занятия периода с названиями зала, тренеров и дисциплин
- [x] 2.3 Зарегистрировать проекцию в `ReadViews` как `schedule`; проверка — `./gradlew :server:compileKotlin` проходит
- [x] 2.4 Тест ограничения филиалом: занятия группы другого филиала не попадают в выдачу; проверка — тест в `server/src/test/kotlin/org/athletica/crm/read/schedule/` проходит
- [x] 2.5 Тест семантики фильтров: несколько значений одного фильтра объединяются по «или», разные фильтры — по «и», пустой фильтр не ограничивает выдачу; проверка — тест на TestPostgres проходит
- [x] 2.6 Тест фильтра по тренеру с переопределённым составом: занятие, где тренер группы заменён на другого, находится по тренеру занятия и не находится по тренеру группы; проверка — тест на TestPostgres проходит
- [x] 2.7 Тест исключения осиротевшего занятия (`group_id IS NULL`); проверка — тест на TestPostgres проходит
- [x] 2.8 Тест, что проекция ничего не создаёт: количество занятий в БД до и после запроса совпадает, повторный запрос того же периода возвращает тот же состав; проверка — тест на TestPostgres проходит

## 3. Резолвер цвета

- [x] 3.1 Реализовать чистую функцию `colorKeyFor(groupId, firstDisciplineId)` в `read/schedule/` — запасная ступень из младших битов UUID (дисциплины, иначе группы) по модулю размера палитры; вызывается из маппера строки `DbScheduleView`; проверка — функция не использует `hashCode()`
- [x] 3.2 Тесты резолвера: ключ стабилен между вызовами, занятия одной дисциплины получают одинаковый ключ, занятие без дисциплины получает ключ по группе; проверка — `./gradlew :server:test --tests "*ScheduleColor*"` проходит

## 4. Роут

- [x] 4.1 Валидация периода в роуте до вызова проекции: `from <= to` и длина не больше 62 дней включительно, иначе `CommonDomainError` (`INVALID_SCHEDULE_PERIOD`, `SCHEDULE_PERIOD_TOO_LONG`); вернуть в `Messages.kt` локализованные сообщения, удалённые в #75 (`InvalidSessionPeriod`, `SessionPeriodTooLong`), под расписание; проверка — тест подтверждает, что период в 62 дня принимается, в 63 и с `to < from` отклоняется, занятия не возвращаются
- [x] 4.2 Нормализация запроса в роуте: пустые списки фильтров превращаются в `null` в `ScheduleQuery`; проверка — запрос без фильтров возвращает все занятия недели филиала
- [x] 4.3 Зарегистрировать `POST /api/schedule/list`, принимающий `ReadViews`, и подключить в `Application.kt`; проверка — запрос к поднятому серверу возвращает занятия периода с названиями зала, тренеров и дисциплин

## 5. API-клиент

- [x] 5.1 Добавить `ScheduleApiClient` с методом запроса расписания и зарегистрировать его в `ApiClient`; проверка — `./gradlew :shared:build` проходит, клиент доступен как `api.schedule`

## 6. Страница расписания

- [x] 6.1 Ввести `ScheduleLoadState` (`Loading`/`Loaded`/`Error`) и перевести `ScheduleState` на единый снимок с неделей, данными, фильтрами и справочниками; проверка — в ViewModel остаётся ровно одна изменяемая ячейка состояния
- [x] 6.2 Загружать занятия при открытии и перезапрашивать при переключении недели, сохраняя выбранные фильтры; проверка — тест ViewModel в `commonTest` подтверждает перезапрос с теми же фильтрами
- [x] 6.3 Загружать справочники залов, дисциплин и сотрудников один раз при открытии страницы; проверка — переключение недели не приводит к повторной загрузке каталогов
- [x] 6.4 Панель фильтров: три фильтра с множественным выбором и сбросом; в фильтре тренеров — все сотрудники каталога (сужение — #74); проверка — значение, отсутствующее в занятиях видимой недели, остаётся доступным для выбора
- [x] 6.5 Карточка занятия: время, продолжительность, название группы, тренеры, зал, дисциплины; строки без данных опускаются целиком, несколько значений перечисляются в одну строку с усечением; проверка — карточка занятия без тренера и дисциплин не показывает пустых подписей; подписи берутся из ответа, а не из справочников фильтров
- [x] 6.6 Продолжительность выводится без нулевой части — только часы либо только минуты; проверка — тест форматирования покрывает 20 минут, 1 час и 1 час 30 минут
- [ ] 6.7 Отменённые занятия отображаются приглушённо и визуально отличимы от запланированных; проверка — видно на экране при наличии отменённого занятия в периоде
- [x] 6.8 Цвет карточки берётся из `SessionColorKey`, неизвестный ключ рисуется нейтральным; проверка — подстановка неизвестного значения не ломает отрисовку карточки
- [x] 6.9 Оставить бейдж свободных мест как заглушку с фиксированным текстом, формируемым на клиенте; проверка — текст не приходит с сервера и одинаков на всех карточках
- [ ] 6.10 Состояния страницы: индикатор загрузки, сообщение об ошибке с повтором, сообщение о пустой неделе и о пустом результате фильтрации; проверка — каждое состояние воспроизводится на экране
- [x] 6.11 Удалить `ScheduleStubData.kt` и остатки локальной генерации данных; проверка — поиск по проекту не находит ссылок на заглушечные данные расписания
- [x] 6.12 Добавить строки локализации в `values/strings.xml` и `values-en/strings.xml`; проверка — в коде страницы нет литеральных пользовательских строк

## 7. Приёмка

- [x] 7.1 Прогнать `./gradlew build` и `./gradlew ktlintFormat`; проверка — обе команды завершаются успешно
- [ ] 7.2 Проверить страницу на поднятом сервере: сетка недели, переключение недель, три фильтра, пустая неделя, ошибка загрузки; проверка — сценарии из `specs/schedule/spec.md` воспроизводятся вручную
- [ ] 7.3 Проверить вёрстку на узком экране: фильтры и сетка остаются доступными; проверка — страница используема при ширине телефона
