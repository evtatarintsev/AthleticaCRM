/**
 * Базовый путь веб-клиента без завершающего `/`: `/web`, пока `/` занимает KMP-клиент,
 * и `""` после переключения. Константа сборки из `vite.config.ts`.
 */
export const BASE_PATH: string = __BASE_PATH__;
